import {StackNavigationProp} from '@react-navigation/stack';
import * as React from 'react';
import {View, Text} from 'react-native';


const FacultyFlow = (props: StackNavigationProp<{}>) => {
	return (
		<View>
			<Text>Faculty</Text>
		</View>
	);
};

export default FacultyFlow;
